{
  "name": "matrice-destiny-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"import('fs').then(fs => { fs.cpSync('public', 'dist', { recursive: true }); console.log('✅ Build OK: public mirrored to dist'); });\"",
    "start": "node server.js",
    "server": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
